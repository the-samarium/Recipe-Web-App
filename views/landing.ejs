<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Landing page</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white text-gray-800 min-h-screen flex flex-col">
  <nav class="bg-white shadow p-4 flex items-center justify-between">
    <span class="text-2xl font-bold text-green-600">Recipe App</span>

    <% if (user) { %>
      <span class="flex justify-center items-center p-2">
        <p>Welcome, <%= user.email %></p>
        <span class="w-3 h-3 rounded-xl"><img src="<%= user.avatar %>" alt=""></span>
        <a href="/logout" class="ml-4 px-4 py-2 bg-red-500 text-white rounded hover:bg-red-600 transition">Logout</a>
      </span>
    <% } else { %>
      <span class="flex justify-center items-center p-2">
        <a href="/login" class="ml-4 px-4 py-2 bg-green-500 text-white rounded hover:bg-red-600 transition">Login</a>
        <a href="/signup" class="ml-4 px-4 py-2 bg-blue-500 text-white rounded hover:bg-red-600 transition">Signup</a>
      </span>
    <% } %>
  </nav>
  <main class="flex-1 flex flex-col items-center justify-center text-center px-4">
    <h1 class="text-4xl md:text-5xl font-extrabold mb-6 mt-12">From Pantry to Plate</h1>
    <p class="text-lg md:text-xl mb-8 text-gray-600 max-w-xl">Discover, Cook, and Savor Recipes Made Just for You! Explore a world of flavors and find your next favorite dish.</p>
    <div class="flex justify-center items-center">
      <img class="w-60 h-60 rounded-md shadow-xl mb-4 hover:scale-105 hover:shadow-2xl transition-transform duration-300"  src="/pic.jpg" alt="">
    </div>
    <% if (!user) { %>
      <a href="/signup" class="inline-block px-8 py-3 bg-green-500 text-white text-lg font-semibold rounded shadow hover:bg-green-600 transition">Get Started</a>
    <% } else { %>
      <a href="/home" class="inline-block px-8 py-3 bg-green-500 text-white text-lg font-semibold rounded shadow hover:bg-green-600 transition">Home</a>
    <% } %>
  </main>
  <footer class="bg-gray-100 text-gray-500 text-center py-4 mt-auto">
    &copy; <span id="year"></span> Recipe App. All rights reserved.
  </footer>
  <script>
    document.getElementById('year').textContent = new Date().getFullYear();
  </script>
</body>
</html>