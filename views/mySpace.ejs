<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Space - Recipe App</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white text-gray-800 min-h-screen flex flex-col">
    <nav class="bg-white shadow p-4 flex items-center justify-between">
        <span class="text-2xl font-bold text-green-600">Recipe App</span>
        <% if (user) { %>
            <span class="flex justify-center items-center">
                <p>Welcome, <%= user.name %></p>
                <a href="/home/new" class="ml-4 px-4 py-2 bg-gray-500 text-white rounded hover:bg-gray-600 transition">Add new</a>
                <a href="/logout" class="ml-4 px-4 py-2 bg-red-500 text-white rounded hover:bg-red-600 transition">Logout</a>
            </span>
        <% } else { %>
            <a href="/login" class="ml-4 px-4 py-2 bg-red-500 text-white rounded hover:bg-red-600 transition">Login</a>
            <a href="/signup" class="ml-4 px-4 py-2 bg-red-500 text-white rounded hover:bg-red-600 transition">Signup</a>
        <% } %>
    </nav>
    <main class="flex-1 flex flex-col items-center px-4">
        <div class="w-full bg-white mt-2 sticky top-0 z-10">
            <h1 class="text-2xl md:text-3xl font-extrabold py-6 text-center">My Uploaded Recipes</h1>
        </div>
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-8 w-full max-w-6xl mx-auto px-4 mt-6 mb-12">
            <% if (recipes.length === 0) { %>
                <div class="col-span-full text-center text-gray-500 text-xl">
                    You haven't uploaded any recipes yet.
                </div>
            <% } else { %>
                <% recipes.forEach((recipe) => { %>
                    <div class="bg-white rounded-lg shadow-md p-6 flex flex-col items-center hover:shadow-lg transition border-double border-2 border-green-400">
                        <div class="w-20 h-20 bg-gray-100 rounded-xl flex items-center justify-center mb-4 border-dotted border-2 border-green-600">
                            <span class="text-base font-bold text-green-600 text-center">
                                <%= recipe.title %>
                            </span>
                        </div>
                        <h2 class="text-xl font-semibold mb-2">
                            <%= recipe.title %>
                        </h2>
                        <p class="text-gray-500 mb-2">Created at: <%= recipe.createdAt %></p>
                        <p class="text-gray-700 mb-2"><%= recipe.process %></p>
                    </div>
                <% }) %>
            <% } %>
        </div>
    </main>
    <footer class="bg-gray-100 text-gray-500 text-center py-4 mt-auto">
        &copy; <span id="year"></span> Recipe App. All rights reserved.
    </footer>
    <script>
        document.getElementById('year').textContent = new Date().getFullYear();
    </script>
</body>
</html> 