<!DOCTYPE html>
<html lang="en">
  
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Recipe - Recipe App</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white text-gray-800 min-h-screen flex flex-col">
  <nav class="bg-white shadow p-4 flex items-center justify-between">
    <span class="text-2xl font-bold text-green-600">Recipe App</span>
  </nav>

  <main class="flex-1 flex flex-col items-center justify-center px-4 mt-3">
    <form action="/home/new" method="POST" class="bg-white rounded-lg shadow-md p-8 w-full max-w-md flex flex-col gap-4 border-solid border-2 border-gray-200">
      <h1 class="text-2xl font-bold mb-4 text-center text-green-700">Add a New Recipe</h1>

      <input type="text" name="title" placeholder="Recipe Title" required 
             class="border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-green-400" />

      <textarea name="ingredients" placeholder="Ingredients (e.g. 1 cup flour, 2 eggs...)" required 
                rows="4"
                class="border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-green-400 resize-none"></textarea>

      <textarea name="procedure" placeholder="Procedure / Steps" required 
                rows="6"
                class="border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-green-400 resize-none"></textarea>

      <button type="submit" class="bg-green-500 text-white py-2 rounded hover:bg-green-600 transition">
        Submit Recipe
      </button>
    </form>
  </main>

  <footer class="bg-gray-100 text-gray-500 text-center py-4 mt-auto">
    &copy; <span id="year"></span> Recipe App. All rights reserved.
  </footer>

  <script>
    document.getElementById('year').textContent = new Date().getFullYear();
  </script>
</body>
</html>
