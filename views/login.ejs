<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Recipe App</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>



<body class="bg-white text-gray-800 min-h-screen flex flex-col">

   <% if (error) { %>
      <script>
        alert("<%= error %>")
      </script>
    <% } %>
    
  <nav class="bg-white shadow p-4 flex items-center justify-between">
    <span class="text-2xl font-bold text-green-600">Recipe App</span>
  </nav>
  <main class="flex-1 flex flex-col items-center justify-center px-4">
    <form action="/login" method="POST" class="bg-white rounded-lg shadow-md p-8 w-full max-w-md flex flex-col gap-4">
      <h1 class="text-2xl font-bold mb-4 text-center">Login</h1>
      <input type="email" name="email" placeholder="Email" required class="border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-green-400" />
      <input type="password" name="password" placeholder="Password" required class="border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-green-400" />
      <input type="file" name="profile" accept="image/*" class="border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-green-400" />
      <button type="submit" class="bg-green-500 text-white py-2 rounded hover:bg-green-600 transition">Login</button>
      <p class="text-center text-gray-500 text-sm">Don't have an account? <a href="/signup" class="text-green-600 hover:underline">Sign up</a></p>
    </form>
  </main>
  <footer class="bg-gray-100 text-gray-500 text-center py-4 mt-auto">
    &copy; <span id="year"></span> Recipe App. All rights reserved.
  </footer>
  <script>
    document.getElementById('year').textContent = new Date().getFullYear();
  </script>
</body>
</html> 